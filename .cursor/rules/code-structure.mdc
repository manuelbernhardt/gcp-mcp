---
description: 
globs: *.py
alwaysApply: false
---
MCP tool implementations are split in the tool method and a helper function. Helper functions are the ones invoking the GCP API (or any other underlying library) whilst the tool function focuses on invoking helper methods and returning the result.